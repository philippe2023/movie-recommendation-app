<!DOCTYPE html>
<html>
  <head>
    <title>MovieRecommendationApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gray-100 flex flex-col h-screen justify-between">
    <!-- Navigation Bar -->
    <nav class="bg-white shadow">
      <div class="container mx-auto px-4 py-4 flex justify-between items-center">
        <div>
          <%= link_to 'Movie Reco', root_path, class: 'text-xl font-bold text-red-600' %>
        </div>
        <%= form_with url: root_path, method: :get, local: true do |form| %>
          <div class="flex">
            <%= form.text_field :query, placeholder: "Search for movies...", class: "w-full px-4 py-2 border rounded-l-md focus:outline-none focus:ring-2 focus:ring-red-500", value: params[:query] %>
            <%= form.submit "Search", class: "px-4 py-2 bg-red-600 text-white rounded-r-md hover:bg-red-700" %>
          </div>
        <% end %>
        <div>
          <%= link_to 'About', about_path, class: 'text-gray-700 hover:text-red-600 mx-2' %>
          <%= link_to 'Contact', contact_path, class: 'text-gray-700 hover:text-red-600 mx-2' %>
        </div>
      </div>
    </nav>

    <!-- Main Content -->
    <%= yield %>

    <!-- Footer (optional) -->
    <footer class="bg-white mt-10">
      <div class="container mx-auto px-4 py-4 text-center text-gray-600">
        &copy; <%= Time.current.year %> Movie Recommendation App
      </div>
    </footer>
  </body>
</html>
